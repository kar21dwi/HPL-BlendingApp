import { TabpressDirective } from './tabpress.directive';

describe('TabpressDirective', () => {
  it('should create an instance', () => {
    const directive = new TabpressDirective();
    expect(directive).toBeTruthy();
  });
});
